{% extends '_layouts/_base.twig' %}

{% block content %}
    <section>
        <h1 class="hero__title_gallery">{{ entry.heading }}</h1>

        <div class="gallery">
            {% for image in entry.sfeerImages.all() %}
                <img src="{{ image.url }}">
            {% endfor %}
        </div>
    </section>

{% endblock %}