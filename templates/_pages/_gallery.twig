{% extends '_layouts/_base.twig' %}

{% block content %}
    <section>
        <h1>{{ entry.heading }}</h1>

        <div>
            {% for image in entry.sfeerImages.all() %}
                <img src="{{ image.url }}" alt="{{ image.title }}">
            {% endfor %}
        </div>
    </section>

{% endblock %}